hl.config({
    plugin = {
        hyprtasking = {
            -- Explicit dark background integer format (e.g., 0xff11111b)
bg_color = 0xff11111b,

select_button = 0x111,
drag_button = 0x110,

gap_size = 10,
border_size = 2,
        }
    }
})

