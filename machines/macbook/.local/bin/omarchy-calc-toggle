#!/usr/bin/env bash
if hyprctl clients -j | jq -e '.[] | select(.class=="omacalc")' >/dev/null 2>&1; then
    hyprctl dispatch 'hl.dsp.window.focus({ match = "class:omacalc" })' >/dev/null
else
    setsid omacalc >/dev/null 2>&1 &
fi
